{% extends "UmbriaProLocoBundle::frontend.html.twig" %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="page-header">
                <h2>Proposte</h2>
            </div>
            <table class="table table-hover table-striped">
                <thead>
                <tr>
                    <th></th>
                    <th>Nome proposta</th>
                    <th><span class="badge pull-right">{{ pagination.getTotalItemCount }}</span></th>
                </tr>
                </thead>
                <tbody>
                {% for proposal in pagination %}
                    <tr>
                        <td class="text-muted">
                            <small>{{ loop.index }}</small>
                        </td>
                        <td>{{ proposal.nomeProposta }}</td>
                        <td>
                            <a class="btn btn-info btn-xs" href="{{ path('proposal_show', { 'id': proposal.id }) }}">show</a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
        <div class="row">
            <div class="pull-right">
                {{ knp_pagination_render(pagination) }}
            </div>
        </div>
    </div>
{% endblock %}